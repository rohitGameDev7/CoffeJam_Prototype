using UnityEngine;

public class TrayColor : MonoBehaviour
{
    public string colorName = "Red"; // Set this in the Inspector to match the tray's type
    public bool hasTeleported = false; // prevent repeat teleports

}
